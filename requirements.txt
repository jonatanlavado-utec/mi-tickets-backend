# Production dependencies (AWS Lambda Python 3.11)
# These will be packaged with Lambda functions

# AWS SDK (included in Lambda runtime, but listed for local development)
boto3>=1.28.0
botocore>=1.31.0

# JWT handling
PyJWT>=2.8.0

# HTTP requests (for API calls - using urllib in stdlib instead)
# requests>=2.31.0  # Uncomment if using requests library

# Development dependencies
# pytest>=7.4.0  # For testing
# pytest-cov>=4.1.0  # For coverage
# moto>=4.2.0  # For AWS mocking in tests